export const parseInput = (calcText: string): number => {
    const clearButton = (clearSum: string, clearText: string, clearOperand: string) => {
        clearSum('');
        clearText('');
        clearOperand('');
    }
}  


export default parseInput;